// Escreva uma função que recebe um array de strings e retorna todas as suas letras em ordem alfabética.

function abc(str){
    return str.sort();
}

console.log(abc(['hello', 'world', 'hi', 'welcome']));